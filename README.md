# BigInteger
This code allows you to work with infinitely large numbers that are not supported by standart libraries C++. It is enough to set them as a string(const char*) using class 'BigInteger' and then perform all possible action.
For output you can use method 'to_string'.
